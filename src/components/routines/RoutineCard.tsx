/**
 * RoutineCard is no longer used - replaced by the inline RoutineItem
 * inside RoutinesPage. Kept as a stub so the file compiles.
 */
export default function RoutineCard(): null {
  return null;
}

